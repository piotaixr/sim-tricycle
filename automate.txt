robot{
	while partie{
		etat;
		scan(vision);
		interroger_carte;
		switch{
			quand {etat==porteur alors switch{quand case==base alors deposer;
							quand case==autre alors aller_a(base);
					    }
			      }
			quand {etat==normal alors switch{quand case==piece alors ramasser;
							quand case==autre aller_a(piece);
				            }
				}
		}
	}
}
fin
