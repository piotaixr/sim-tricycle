/*fichier automate*/
robot{

	etat==normal{
		Si piece_trouvee alors etat=deplacement_norm;
		Sinon{ attente(temps);
		       etat=normal;
		}
	}
	etat==deplacement_norm{
		Si case==piece alors{ ramasser(temps);
				     etat=porteur;
			      }
		Sinon{aller(piece,temps);
		      etat=normal;
		}
	}
	etat==porteur{
		etat=deplacement_port
	}
	etat==deplacement_port{
		Si case==base alors{deposer(temps);
				   etat=normal;
			     }
		Sinon{aller(base,temps);
		      etat=deplacement_port;
		}
	}
}
fin
